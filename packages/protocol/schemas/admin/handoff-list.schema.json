{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "type": "object",
  "required": [
    "data",
    "pagination"
  ],
  "properties": {
    "data": {
      "maxItems": 100,
      "type": "array",
      "items": {
        "additionalProperties": false,
        "type": "object",
        "required": [
          "handoffId",
          "tenantId",
          "siteId",
          "conversationId",
          "runId",
          "status",
          "createdAt",
          "updatedAt"
        ],
        "properties": {
          "handoffId": {
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$",
            "type": "string"
          },
          "tenantId": {
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$",
            "type": "string"
          },
          "siteId": {
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$",
            "type": "string"
          },
          "conversationId": {
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$",
            "type": "string"
          },
          "runId": {
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$",
            "type": "string"
          },
          "status": {
            "anyOf": [
              {
                "const": "requested",
                "type": "string"
              },
              {
                "const": "awaiting_contact",
                "type": "string"
              },
              {
                "const": "delivering",
                "type": "string"
              },
              {
                "const": "completed",
                "type": "string"
              },
              {
                "const": "failed",
                "type": "string"
              }
            ]
          },
          "createdAt": {
            "format": "date-time",
            "pattern": "Z$",
            "type": "string"
          },
          "updatedAt": {
            "format": "date-time",
            "pattern": "Z$",
            "type": "string"
          }
        }
      }
    },
    "pagination": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object",
          "required": [
            "hasMore",
            "nextCursor"
          ],
          "properties": {
            "hasMore": {
              "const": true,
              "type": "boolean"
            },
            "nextCursor": {
              "minLength": 1,
              "maxLength": 512,
              "pattern": "^[A-Za-z0-9_-]+$",
              "type": "string"
            }
          }
        },
        {
          "additionalProperties": true,
          "type": "object",
          "required": [
            "hasMore"
          ],
          "properties": {
            "hasMore": {
              "const": false,
              "type": "boolean"
            },
            "nextCursor": {
              "minLength": 1,
              "maxLength": 512,
              "pattern": "^[A-Za-z0-9_-]+$",
              "type": "string"
            }
          }
        }
      ]
    }
  }
}
